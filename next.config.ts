import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['neutral-juliet-isdl-hackathon-7d137747.koyeb.app', 'localhost'],
  },
};

export default nextConfig;
